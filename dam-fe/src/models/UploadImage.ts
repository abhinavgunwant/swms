export interface UploadImage {
    uploadId: string,
    name: string,
    title: string,
    slug: string,
    encoding: string,
    projectId: number,
    folderId: number,
}

export default UploadImage;

