import {
    Card, TextField
} from '@mui/material';

import Typeahead from './Typeahead';

const SelectUsers = () => {
    return <Card>
        <Typeahead placeholder="Type names to add users" />
        this content appears right under
    </Card>
}

export default SelectUsers;
